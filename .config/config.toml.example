[general]
confirm_deletes = true
max_workers = 8
locale = "de-DE"

[paths]
base_documents = "<auto:OS>"       # OS‑spezifisch ermittelt
base_downloads = "<auto:OS>"
base_desktop   = "<auto:OS>"

[downloads]
session_prefix = "session"
use_timestamp_subfolders = true

[planner]
use_mime_sniffing = true
use_ai = false

[undo]
manifest_dir = "<auto:OS>"